  int num;
  printf("Enter number:");
  scanf("%d",&num);
  if(num>0)
    printf("number is  positive",num);
  if(num<0)
    printf("number is  negative");

  else
    printf("number is zero");
return 0;
